# §M optional allowlist（scripts/check-secrets-coverage.py 讀取）
# runtime 會讀、但缺值不影響核心功能的 SECRETISH 名（10-SECRETS-CONTRACT §5.2 try* accessor 模式）。
# 對應 .portability.toml [secrets].optional_worker——兩處同步（手動維護）。
SLACK_API_TOKEN
